diff --git StarFile.html StarFile.html
index ebbe854..c5aa5ed 100644
--- StarFile.html
+++ StarFile.html
@@ -92,6 +92,13 @@ from types import *
 from urllib import *         # for arbitrary opening
 import re
 import copy
+# For Python 2.6 or higher compatibility
+try:
+    set
+except NameError:
+    import sets
+    set = sets.Set
+
 <a name="NW3RaCNp-1p0Y9w-1-u2" href="#NWD3RaCNp-E"><i>&lt;Define a collection datatype&gt;</i></a>
 <a name="NW3RaCNp-1p0Y9w-1-u3" href="#NWD3RaCNp-F"><i>&lt;LoopBlock class&gt;</i></a>
 <a name="NW3RaCNp-1p0Y9w-1-u4" href="#NWD3RaCNp-n"><i>&lt;StarBlock class&gt;</i></a>
@@ -492,7 +499,7 @@ def ReadStar(filename,maxlength=2048,dest=StarFile(),scantype='standard',grammar
             realurl = &quot;file:&quot; + filename
         else:
             realurl = filename
-        filestream = urlopen(filename)
+        filestream = urlopen(realurl)
     else:
         filestream = filename   #already opened for us
     my_uri = &quot;&quot;
@@ -1133,9 +1140,8 @@ as well).
 <p>
 <pre><a name="NW3RaCNp-3lrZ7G-1" href="#NWD3RaCNp-Y"><dfn>&lt;Audit for repeated names&gt;=</dfn></a> <b>(<a href="#NWD3RaCNp-F">&lt;-U</a>)</b>
 def audit(self):
-    import sets
     allkeys = self.keys()
-    uniquenames = sets.Set(allkeys)
+    uniquenames = set(allkeys)
     if len(uniquenames) == len(allkeys): return []
     else:              
         keycount = map(lambda a:(a,allkeys.count(a)),uniquenames)
diff --git StarFile.py StarFile.py
index 6099ec7..204c222 100644
--- StarFile.py
+++ StarFile.py
@@ -37,6 +37,13 @@ from types import *
 from urllib import *         # for arbitrary opening
 import re
 import copy
+# For Python 2.6 or higher compatibility
+try:
+    set
+except NameError:
+    import sets
+    set = sets.Set
+
 class StarList(list):
     pass
 
@@ -541,9 +548,8 @@ class LoopBlock(object):
         return newlb
         
     def audit(self):
-        import sets
         allkeys = self.keys()
-        uniquenames = sets.Set(allkeys)
+        uniquenames = set(allkeys)
         if len(uniquenames) == len(allkeys): return []
         else:              
             keycount = map(lambda a:(a,allkeys.count(a)),uniquenames)
@@ -1022,7 +1028,7 @@ def ReadStar(filename,maxlength=2048,dest=StarFile(),scantype='standard',grammar
             realurl = "file:" + filename
         else:
             realurl = filename
-        filestream = urlopen(filename)
+        filestream = urlopen(realurl)
     else:
         filestream = filename   #already opened for us
     my_uri = ""
